<view class="con">
    <view class="title">左下角</view>
    <view wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:for-item="i">
        <view class="inline" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:for-item="j">
            <view wx:if="{{j<=i}}">
                {{i}}*{{j}}={{i*j}}
            </view>
        </view>
    </view>
    <view class="title">右上角</view>
    <view wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:for-item="i">
        <view class="inline" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:for-item="j">
            <view wx:if="{{j>=i}}">
                {{i}}*{{j}}={{i*j}}
            </view>
        </view>
    </view>
    <view class="title">左上角</view>
    <view wx:for="{{[9,8,7,6,5,4,3,2,1]}}" wx:for-item="i">
        <view class="inline" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:for-item="j">
            <view wx:if="{{j<=i}}">
                {{i}}*{{j}}={{i*j}}
            </view>
        </view>
    </view>
    <view class="title">右下角</view>
    <view wx:for="{{[9,8,7,6,5,4,3,2,1]}}" wx:for-item="i">
        <view class="inline" wx:for="{{[1,2,3,4,5,6,7,8,9]}}" wx:for-item="j">
            <view wx:if="{{j>=i}}">
                {{i}}*{{j}}={{i*j}}
            </view>
        </view>
    </view>
</view>